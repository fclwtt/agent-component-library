name: LLMCalling
description: LLM调用接口
methods:
- name: call_llm
  params:
    messages: List[Dict]
    model: str
    '**kwargs': Any
  returns: Dict
  description: 调用LLM
- name: async_call_llm
  params:
    messages: List[Dict]
    model: str
    '**kwargs': Any
  returns: Dict
  description: 异步调用LLM
