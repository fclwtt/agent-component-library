name: Agent Engine
id: agent-engine
description: Agent核心引擎，包含Agent循环、工具执行、Prompt构建、上下文管理等核心功能
category: core
version: 1.0.0
implementations:
  hermes:
    source: NousResearch/hermes-agent
    commit: '503867864'
    modules: 134
interfaces:
  provides: ['context-management']
  requires: ['llm-calling', 'tool-registry', 'memory-provider']
dependencies: []
