[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "magic-orange-component"
version = "0.1.0"
description = "MagicOrange框架拆解组件"
requires-python = ">=3.10"
