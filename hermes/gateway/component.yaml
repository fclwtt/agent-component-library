name: Gateway
id: gateway
description: 消息网关，包含消息路由、平台适配（Telegram、Discord、微信等）、会话管理、消息投递
category: infrastructure
version: 1.0.0
implementations:
  hermes:
    source: NousResearch/hermes-agent
    commit: '503867864'
    modules: 65
interfaces:
  provides: ['platform-registry']
  requires: ['tool-registry', 'memory-provider']
dependencies: []
